`````
pip install opencv-python mediapipe
`````
````
python3 -m venv venv
````
`````
source venv/bin/activate
`````
`````
sudo systemctl stop unattended-upgrades && sudo apt update && sudo apt install -y openssh-server && sudo systemctl enable --now ssh && sudo ss -tlnp | grep :22

`````
